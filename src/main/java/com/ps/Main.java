package com.ps;

public class Main {
    public static void main(String[] args) {
        //This creates an instance of UserInterface to manage allg the user interactions.
        UserInterface UI = new UserInterface();

        // This displays the user interface and handles all user commands.
        UI.display();
    }
}